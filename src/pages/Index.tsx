import React from 'react';

const Index = () => {
  return (
    <div>
      <h1>Welcome to the Index Page</h1>
      <style>
        {`
          @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
          }
        `}
      </style>
    </div>
  );
};

export default Index;
